/** @jsxRuntime classic */
/** @jsx jsx */
import { jsx, Box, Text } from "theme-ui";

export default function SkillBox({ lable, color }) {
  return (
    <Box sx={{}}>
      <Text>{lable}</Text>
    </Box>
  );
}
